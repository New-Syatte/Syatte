import RouteComplete from "@/utils/RouteComplete";
const page = () => {
  return (
    <RouteComplete>
      <div className="text-3xl text-bold">ModernMasters</div>
    </RouteComplete>
  );
};

export default page;
